name: Emotes
version: '${project.version}'
main: exposed.hydrogen.emotes.emotes.Emotes
api-version: 1.18
prefix: emotes
authors: [ Hydrogen ]
depend: [ Resources ]
website: hydrogen.exposed

permissions:
  emotes.use:
    description: "Allows the player to use emotes."
    default: true
  emotes.staff:
    description: "Allows the player to manage emotes."
    default: op